# v4.7.10-r7 snapshot — echo-guard v4.0 token-array

**版本**: v4.7.10-r7
**echo-guard**: v4.0 (token-array destruct-vet — regex→Set)
**hooks.test**: 38/38 — 40 checks in source
**HEAD**: cc2eda2

**v4.0 变更** (rubber-band root fix):
- destruct-vet从正则枚举flag改为token数组Set匹配(READONLY_COMMANDS + DESTRUCTIVE_FLAGS + READONLY_WITH_POTENTIAL_FLAGS)
- 类比test_security.py的Set-based验证——新增flag只需加一行到Set,无需验证正则边界
- isReadonly()/isIdempotent()/isPure()三个token-based函数替代原L99-117的匿名正则
- 保留原有ECHO_BLOCK/wc检查/指纹/Cap/GC机制不变

**验证矩阵** (all pass):
- grep/cat/find*.py/sort(no flags)→exempt
- find -exec/-execdir/-delete/-fprintf→非exempt
- sort -o(flag-first/flag-last)/--output→非exempt
- 所有现有hooks.test regression 38/38 ✔

**收敛展望**: rubber-band trap已断——token-array替代regex枚举后不再有flag边界歧义。预计Scan29=1/2[3A]→Scan30=2/2→50A终验→签收。

<!-- SNAPSHOT-COMPLETE 20260718T2000 -->
