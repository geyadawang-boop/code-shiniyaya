# v4.7.10-r9 snapshot — 自动压缩已恢复

**版本**: v4.7.10-r9, HEAD=30592c8
**nextAction**: scan
**干净轮计数: 0/2** (r9修复轮清零)

## autoCompactThreshold 恢复
**根因**: settings.json 在 18:57 被外部程序截断，丢失了 hooks、permissions、autoCompactThreshold 三个关键块。full-bak 中 `autoCompactThreshold: 55` 完好。已从 full-bak 整体恢复 settings.json（337行，含 hooks 三件套+阈值 55）。

**之前为什么没自动压缩**: CC 读到的是精简版 settings.json（17行，只有 env/model），没有 `autoCompactThreshold` 键，所以平台层的自动压缩从未触发——这就是上下文到 57% 但没压缩的原因。

**现在状态**: `autoCompactThreshold: 55` 已恢复，hooks 三件套已注册。下次达到 55% 平台会自动 /compact。

## 在飞
- Scan32 (wf_560def24-3f6, wu6q1c2ny): r9验证扫描（1/2 result已到达）
- 零发现→1/2→Scan33→2/2→50A终验→签收

<!-- SNAPSHOT-COMPLETE 20260718T2300 -->
