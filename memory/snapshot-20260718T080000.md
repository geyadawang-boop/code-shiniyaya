# v4.7.9-r5 snapshot — Scan 9 在飞 + 预发射失败诊断

**版本**: v4.7.9-r5, HEAD=15b4d0e
**nextAction**: await
**干净轮计数: 0/2** (r5=修复轮已清零)

## 诊断：r4 turn 写了进度行但未发射 Agent
- r4 commit 后我输出"⚡ 第9轮[6A]: 最后轮换扫描在飞→继"但实际未调用 Workflow
- 根因：文本行写了但工具调用未执行——不是 stop-guard 拦截（没有收敛/干净轮声明触发门），是模型在 turn 末尾自发提前停止
- 预防：规则 24 预发射协议要求同 turn 内"commit+进度行+Workflow 调用"三连——Workflow 调用必须在输出进度行之前执行
- Scan 9 已由当前 turn 正确发射（wf_9810e020-485, 6 维）

## r5 已完成 (HEAD=15b4d0e): Scan 8 战果
1. **P0**: echo-guard READONLY 豁免 find -delete/sort -o 旁路——regex `\b-delete\b` 永远不匹配（-是非单词字符），改为 `(?:^|\s)-delete\b`。实测修复前 `find . -delete` 12 次连续免拦
2. **P1**: exempt 调用不刷 lastTs 冻住 cap 窗口——双时间戳 lastTs（所有调用）/ _capTs（仅非豁免）
3. **P2**: SKILL.md echo-guard v3.0→v3.3 全站 + stop-guard v3.2 描述同步 + 台账收敛约束
4. hooks.test 30/30

## 恢复后决策
- ① Scan 9 结果到→先处理（journal: wf_9810e020-485/journal.jsonl）
- 零确认→干净轮1/2→发射 Scan 10（最后维度：18自检/拒绝台账/消毒流水线/README/三hook联合演练——Scan 8 的维度 3/4/5/6）
- 有确认发现→修复→清零

## 关键约定
- Git: commit 不 push; settings 恢复源=full-bak
- 预发射纪律：Workflow 调用先于进度行输出——防再次"写行不发射"

<!-- SNAPSHOT-COMPLETE 20260718T0800 -->
